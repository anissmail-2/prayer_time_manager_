<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Mirrors AwkatiColors.backgroundLight / primaryLight in core:designsystem
         (XML resources can't reference Kotlin constants, so these two are
         kept in sync by hand -- there are only two, that's an acceptable cost). -->
    <color name="window_background">#F6F8F7</color>
    <color name="launcher_background">#0F766E</color>
</resources>
